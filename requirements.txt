# --- 공통 / 설정 ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2

# --- 백엔드 (backend/) ---
fastapi==0.115.5
uvicorn[standard]==0.32.0

# --- 에이전트 (agent/) ---
# google-adk 패키지명/버전은 변동이 잦으므로 설치 전 PyPI에서 최신 확인 필요
google-adk
google-generativeai==0.8.3

# --- 정형 데이터 (db/) ---
# sqlite3는 파이썬 표준 라이브러리 포함, 별도 설치 불필요

# --- 벡터DB / 임베딩 (rag/) ---
chromadb==0.5.20
sentence-transformers==3.2.1
google-cloud-aiplatform==1.71.1

# --- PDF 파싱 (pipeline/) ---
pdfplumber==0.11.4

# --- 평가 (eval/) ---
ragas==0.2.6
deepeval==2.0.9

# --- 개발 편의 ---
python-multipart==0.0.12
